<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- BootSplashTheme để cấu hình Splash Screen -->
    <style name="BootSplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/bootsplash</item> <!-- Đảm bảo có dòng này -->
    </style>

</resources>
